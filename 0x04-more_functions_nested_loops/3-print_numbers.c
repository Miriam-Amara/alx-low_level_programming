#include "main.h"

/**
 *print_numbers - prints from 0 to 9 and a new line at the enf
 *Returns: nothing i.e void
 */

void print_numbers(void)
{
int x;
for (x = 0; x <= 0; x++)

{
int a;
for (a = 0; a <= 9; a++)

_putchar('0' + a);
_putchar('\n');
}
}
