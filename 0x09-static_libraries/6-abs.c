#include "main.h"

/**
 *_abs - prints the absolute value of numbers
 *@r: is the parameter to be checked
 *Return: 0
 */

int _abs(int r)
{
if (r < 0)
{
r = -r;
return (r);
}
else
{
return (r);
}
return (0);
}
